#!/bin/bash

function build {
        echo "This serves as an super evil example."
        sleep 1
}

function default {
        build
}

function help {
        echo "$0 <task> <args>"
        echo "Tasks:"
        compgen -A function | cat -n
}

TIMEFORMAT="Task completed in %3lR"

time ${@:-default}

